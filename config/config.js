'use strict';

const config = {
    port: 8080,
    database: {
        name: 'taptap',
        username: 'root',
        password: 'usbw',
        host: "localhost",
        port: 3307,
        dialect: 'mysql'
    }
};

module.exports = config;
